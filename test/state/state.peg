{
package main

func main() {
	in := os.Stdin
	if len(os.Args) > 1 {
		f, err := os.Open(os.Args[1])
		if err != nil {
			log.Fatal(err)
		}
		defer f.Close()
		in = f
	}
	got, err := ParseReader("", in, Debug(true))
	fmt.Println(got, err)
}

}

start = #{ state["countCs"] = 0;return nil} ((x/y) ws*)* { return state["countCs"],nil }

x = "ab" c "d"
y = "a" bc "e"

c = "c" #{ state["countCs"] = state["countCs"].(int)+3; return nil }
bc = "bc" #{ state["countCs"] = state["countCs"].(int)+1; return nil}

ws = " " / "\n"
